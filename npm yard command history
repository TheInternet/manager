npm install --save-dev eslint-config-rallycoding
// create .eslintrc at top level outside of /src with:
// not yet clear this worked?
/*
{
    "extends": "rallycoding"
}
*/

npm install --save react-redux redux

npm install --save firebase
// also requires setup in firebase and putting web setup code in App.js

npm install --save redux-thunk

npm install --save react-navigation

npm install --save prop-types
/*
You can use prop-types to document the intended types of properties passed to components.
React (and potentially other libraries—see the checkPropTypes() reference below) will check
props passed to your components against those definitions, and warn in development if they don’t match.
*/

npm install --save lodash
// has helper methods for working with objects and arrays

npm install react-native-elements --save
// lots of UI stuff